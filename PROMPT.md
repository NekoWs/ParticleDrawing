# ParticleDrawing 代码块编写指南（PROMPT）

> 面向 AI 助手的参考文档：讲解「函数对象代码块」的结构、语法与全部可用函数，用于学习编写粒子动画代码块。
> 代码块是一种轻量表达式语言，在播放动画时**对每个粒子实时求值**，计算其位置、颜色、速度、缩放、发光与光照等级。

---

## 1. 代码块是什么

一个函数对象（Function Object）描述「一组派生粒子如何生成」。它包含以下字段：

| 字段 | 说明 |
|---|---|
| `code` | **代码块**：分号分隔的赋值语句序列，逐语句执行 |
| `vars` | 变量表：`{ 变量名: { expr: 表达式, kf: 关键帧列表 } }`，可在代码块内通过变量名引用 |
| `count` | 采样数（决定内置变量 `n` 的值） |
| `center` | 整体中心偏移 `[cx, cy, cz]` |
| `style` | 粒子样式（DOT / DUST / FLAME 等） |
| `duration` / `step` | 派生轨道采样参数（编辑器端用） |

代码块对 `i = 0 .. n-1` 的每个粒子各执行一次，写出该粒子的属性。

---

## 2. 基本结构

- 代码块 = 若干**赋值语句**，用 `;`（或换行）分隔。
- 每条语句形如 `目标 = 表达式`，从上到下顺序执行。
- 后续语句可引用前面语句定义的**临时变量**。

```
th = acos(1 - 2 * (i + 0.5) / n);
ph = i * pi * (3 - sqrt(5));
[x, y, z] = [rad * sin(th) * cos(ph), rad * cos(th), rad * sin(th) * sin(ph)];
[r, g, b, a] = [1, 1, 1, 1];
glow = 1;
light = 12;
```

---

## 3. 内置变量与常量

| 名称 | 含义 |
|---|---|
| `i` | 粒子序号，从 `0` 到 `n-1` |
| `n` | 粒子总数（= `count`） |
| `t` | 当前时间 tick（随动画播放递增） |
| `pi` | 圆周率 π |
| `e` | 自然常数 e |

> 约束：`i`、`n`、`t` 是保留字，**只能读取，不可赋值**。

---

## 4. 输出属性（赋值目标）

| 属性 | 含义 | 初始值 | 求值后处理 |
|---|---|---|---|
| `x` `y` `z` | 位置分量 | 0 | 无 |
| `r` `g` `b` `a` | 颜色分量 | 1 | clamp 到 `[0, 1]` |
| `vx` `vy` `vz` | 速度分量 | 0 | 无 |
| `sc` | 缩放 | 1 | clamp 到 `>= 0.01` |
| `glow` | 发光 | 0（false） | `> 0.5` 视为开启（true） |
| `light` | 光照等级 | 0 | 取整并 clamp 到 `[0, 15]` |

这些名字是**属性保留字**，不可用作变量名或临时变量名。

---

## 5. 赋值形式

代码块支持四种赋值形式：

**① 打包赋值（多个标量同时赋值）**

```
[x, y, z] = [expr1, expr2, expr3];
[r, g, b, a] = [expr1, expr2, expr3, expr4];
[vx, vy, vz] = [expr1, expr2, expr3];
```

**② 向量拆包（把单个向量拆给三个标量）**

```
v = vec(1, 2, 3);
[x, y, z] = v;
```

**③ 单属性赋值**

```
sc = 2.0;
glow = 1;
light = 12;
x = i / n - 0.5;
```

**④ 临时变量（非属性、非保留字的名字，先定义后使用）**

```
th = acos(1 - 2 * (i + 0.5) / n);
```

---

## 6. 运算符与优先级

| 优先级 | 运算符 | 结合性 | 说明 |
|---|---|---|---|
| 3（最高） | `^` | 右结合 | 幂 |
| 2.5 | 一元 `-` | — | 负号（`-2^2 = -(2^2)`） |
| 2 | `*` `/` `%` | 左结合 | 乘 / 除 / 取余 |
| 1（最低） | `+` `-` | 左结合 | 加 / 减 |

可用括号 `( )` 改变优先级，函数参数用逗号 `,` 分隔。

**类型规则**（标量 `s`、向量 `v`、矩阵 `m`）：

| 运算 | 结果 |
|---|---|
| `s + s` `s - s` `s * s` `s / s` `s % s` `s ^ s` | 标量 |
| `v + v` `v - v` | 向量（逐分量） |
| `v * s` `s * v` `v / s` | 向量（缩放） |
| `v * v` | 向量（逐分量相乘，Hadamard） |
| `m * v` | 向量（矩阵变换） |
| `m * m` | 矩阵（矩阵相乘） |
| `m * s` `s * m` | 矩阵（缩放） |
| `m + m` `m - m` | 矩阵（逐元素） |

---

## 7. 函数清单（共 39 个）

### 7.1 标量函数（27 个）

| 函数 | 参数 | 说明 |
|---|---|---|
| `sin(x)` | 1 | 正弦（弧度） |
| `cos(x)` | 1 | 余弦（弧度） |
| `tan(x)` | 1 | 正切（弧度） |
| `asin(x)` | 1 | 反正弦 |
| `acos(x)` | 1 | 反余弦 |
| `atan(x)` | 1 | 反正切 |
| `atan2(y, x)` | 2 | 反正切（`y/x`，注意先 y 后 x） |
| `sqrt(x)` | 1 | 平方根 |
| `abs(x)` | 1 | 绝对值 |
| `sign(x)` | 1 | 符号：正 1 / 负 -1 / 零 0 |
| `exp(x)` | 1 | `e^x` |
| `log(x)` | 1 | 自然对数（以 e 为底，与 `ln` 等价） |
| `ln(x)` | 1 | 自然对数（与 `log` 等价） |
| `floor(x)` | 1 | 向下取整 |
| `ceil(x)` | 1 | 向上取整 |
| `round(x)` | 1 | 四舍五入 |
| `fract(x)` | 1 | 取小数部分（`x - floor(x)`） |
| `pow(a, b)` | 2 | `a^b` |
| `min(a, b)` | 2 | 较小值 |
| `max(a, b)` | 2 | 较大值 |
| `clamp(x, lo, hi)` | 3 | 把 `x` 限制在 `[lo, hi]` |
| `lerp(a, b, t)` | 3 | 线性插值 `a + (b - a) * t` |
| `step(edge, x)` | 2 | `x >= edge` 返回 1，否则 0 |
| `smoothstep(e0, e1, x)` | 3 | 平滑阶跃：`x` 在 `[e0, e1]` 归一化后 `t*t*(3-2t)` |
| `mod(a, b)` | 2 | 取模（GLSL 语义：`a - b * floor(a / b)`） |
| `random()` | 0 | 随机数 `[0, 1)`（每帧变化） |
| `rand(seed)` | 1 | 确定性伪随机：同一 `seed` 恒返回相同 `[0, 1)` 值 |

### 7.2 向量函数（5 个）

| 函数 | 参数 | 说明 |
|---|---|---|
| `vec(x, y, z)` | 3 | 由三个标量构造向量 |
| `dot(a, b)` | 2 | 向量点积（返回标量） |
| `cross(a, b)` | 2 | 向量叉积（返回向量） |
| `len(v)` | 1 | 向量长度（标量输入时等价于 `abs`） |
| `norm(v)` | 1 | 向量归一化（单位向量） |

### 7.3 矩阵函数（4 个）

| 函数 | 参数 | 说明 |
|---|---|---|
| `rotX(t)` | 1 | 绕 X 轴旋转矩阵（弧度） |
| `rotY(t)` | 1 | 绕 Y 轴旋转矩阵（弧度） |
| `rotZ(t)` | 1 | 绕 Z 轴旋转矩阵（弧度） |
| `rotAxis(axis, t)` | 2 | 绕任意向量轴旋转矩阵（弧度） |

### 7.4 坐标构造函数（3 个）

| 函数 | 参数 | 说明 |
|---|---|---|
| `polar(r, a)` | 2 | 极坐标 → 向量 `(r*cos(a), 0, r*sin(a))`（XZ 平面） |
| `sphere(r, th, ph)` | 3 | 球坐标 → 向量 `(r*sin(th)*cos(ph), r*cos(th), r*sin(th)*sin(ph))` |
| `torus(R, r, th, ph)` | 4 | 环面坐标 → 向量 `((R+r*cos(th))*cos(ph), r*sin(th), (R+r*cos(th))*sin(ph))` |

---

## 8. 向量与矩阵

- 向量为 `vec3`（三标量），矩阵为 `mat3`（3×3，行主序）。
- **分量访问**：用后缀 `.x` / `.y` / `.z` 读取向量分量，例如 `v = vec(1, 2, 3); x = v.x;`。
- 典型用法：先构造旋转矩阵，再用 `m * v` 变换向量，最后拆包写回位置：

```
m = rotY(t);
p = vec(cos(i), 0, sin(i));
q = m * p;
[x, y, z] = q;
```

---

## 9. 变量系统（vars）

`vars` 里的每个变量有两个字段：

| 字段 | 说明 |
|---|---|
| `expr` | 表达式字符串（无关键帧时求值） |
| `kf` | 关键帧列表，形如 `[[tick, value, easing], ...]` |

- **关键帧优先**：若 `kf` 非空，则按关键帧插值取值（随 `t` 变化），忽略 `expr`。
- 变量 `expr` 可引用：`i` / `n` / `t`、其他变量（支持链式引用）。
- 变量名不能与属性保留字（`x` `y` `z` `r` ... `light`）同名。
- 变量间不能循环引用（`a` 引用 `b`，`b` 又引用 `a` 会报错）。

---

## 10. 完整示例

以下示例摘自内置预设，展示「变量 + 代码块」的典型组合。

**球体（sphere）**

```
vars: { rad: { expr: "3", kf: [] } }
code:
  th = acos(1 - 2 * (i + 0.5) / n);
  ph = i * pi * (3 - sqrt(5));
  [x, y, z] = [rad * sin(th) * cos(ph), rad * cos(th), rad * sin(th) * sin(ph)];
  [r, g, b, a] = [1, 1, 1, 1];
  glow = 1;
  light = 12;
```

**圆环（torus）**

```
vars: { major: { expr: "3", kf: [] }, minor: { expr: "1", kf: [] }, m: { expr: "24", kf: [] }, k: { expr: "12", kf: [] } }
code:
  th = i % k / k * 2 * pi;
  ph = floor(i / k) / m * 2 * pi;
  [x, y, z] = [(major + minor * cos(th)) * cos(ph), minor * sin(th), (major + minor * cos(th)) * sin(ph)];
  [r, g, b, a] = [1, 1, 1, 1];
  glow = 1;
  light = 10;
```

**星形（star）**

```
vars: { rad: { expr: "20", kf: [] } }
code:
  m = floor(pow(n, 0.5));
  u = floor(i / m) * 2 * pi / m;
  v = i % m * pi / m - pi / 2;
  x = rad * pow(cos(u) * cos(v), 3);
  y = rad * pow(sin(u) * cos(v), 3);
  z = rad * pow(sin(v), 3);
```

---

## 11. 约束与最佳实践

1. **优先纯标量**：只使用标量函数、四则运算与 `[x,y,z]=[...]` / `[r,g,b,a]=[...]` 打包赋值，避免向量/矩阵函数（`vec`/`dot`/`cross`/`len`/`norm`/`rotX`/`rotY`/`rotZ`/`rotAxis`/`polar`/`sphere`/`torus`）、`.x/.y/.z` 分量访问、以及 `[x,y,z]=向量` 拆包。纯标量代码块在游戏端走**编译快路径**（5 万粒子个位数毫秒），含向量/矩阵则回退解释器（慢数倍）。
2. **不修改保留字**：`i` / `n` / `t` 只能读取，不可作为赋值目标。
3. **变量名合法**：不与属性保留字（`x` `y` `z` `r` `g` `b` `a` `vx` `vy` `vz` `sc` `glow` `light`）冲突，不循环引用。
4. **临时变量先定义后使用**：后续语句只能引用前面已赋值的临时变量。
5. **颜色范围**：`r/g/b/a` 通常取值 `[0, 1]`（会被 clamp）；`light` 取整数 `[0, 15]`；`glow` 取 `0` 或 `1`。
6. **用确定性随机**：需要稳定外观时用 `rand(seed)`（同种子同结果），需要每帧变化时用 `random()`。
